# Changelog

## Unreleased

## v1.5.5

- 修复安装目录浏览窗口左下角“新建文件夹”按钮不显示的问题。
- 选择安装目录时可直接新建文件夹并继续安装，安装路径选择行为保持不变。

## v1.5.4

- 安装向导完整切换为简体中文，显示软件 Logo、功能简介和作者“称心”。
- 默认安装目录为 D:\DXTQH；未检测到 D 盘时自动回退至 C:\Program Files\DXTQH，安装时仍可自行修改。
- “创建桌面快捷方式”现在默认勾选；开始菜单、已安装应用和程序元数据统一显示正式名称“多系统切换”。

## v1.5.3

- 新增标准 Windows 安装包 `DualBootSwitcher-Setup.exe`，安装时可选择目录，并可选创建桌面快捷方式。
- 安装完成后自动创建“多系统切换”开始菜单入口，可直接启动或从 Windows 设置中卸载。
- GitHub Release 同步提供安装包、单文件便携版和便携 ZIP；软件内更新仍只下载并校验主程序，保留原安装目录和快捷方式。

## v1.5.2

- 替换应用 Logo 和 EXE 图标为新的双系统切换高清图标。
- 保留透明边缘和多尺寸 ICO 输出，适配窗口、任务栏和桌面快捷方式。

## v1.5.1

- 修复启动项列表表头直角边框与卡片不一致的问题，统一为圆润过渡。
- 优化启动项列表的列布局、行高和选中状态标记，减少拥挤和错位。
- 支持修改启动项显示名称并恢复 Windows 原名称。
- 收紧启动项与默认系统操作的状态同步，保留远程更新和公告功能。

## v1.5.0

- 将主界面重构为浅色绿色任务工作区：顶部启动状态条、系统列表与目标详情分栏，窄窗口自动上下堆叠。
- 为启动项选择增加可中断的浅绿色选中反馈和左侧状态标记，保持行高与布局稳定。
- 删除旧蓝色 Banner 令牌并统一按钮、焦点、选中态和详情标签的绿色视觉令牌。
- 同步更新设计规范与 README，明确状态条、分栏布局和减少动画行为。

## v1.4.2

- 删除高风险的外部启动入口及全部相关代码、测试和文档。
- 仅保留 Windows BCD 默认启动项和启动等待时间设置，避免程序再次写入一次性外部启动配置。
- 修复远程更新解析异常和非 HTTPS 下载地址校验问题。

## v1.4.0

- 统一公告、备注和启动等待时间窗口：使用 12px 圆角应用外壳、自绘标题栏、细边框及一致的蓝灰层级，替换默认 WinForms 直角对话框。
- 删除左侧导航栏，主工作区改为整窗宽度；版本与更新状态迁入顶部项目动态区域。
- 恢复顶部“查看公告”按钮，继续自动同步公告摘要并支持打开完整 Markdown 公告。
- 修复旧根窗口分隔线穿过 Inspector 状态文字和操作按钮的问题，操作区改为独立固定面板。
- 按 macOS HIG 思路重构为单页主控台，取消分散页面。
- 左侧改为 200px 图标与文字导航，底部常驻版本和云更新状态；启动等待时间直接放入系统列表工具栏，不再提供独立偏好页面或抽屉。
- 顶部融合当前默认启动环境与公告摘要；公告静默刷新，点击摘要打开完整 Markdown 弹窗。
- 系统列表改为无硬边框圆角行，备注使用 Pill 标签，状态使用绿灰圆点；双击备注可就地编辑，Enter 保存、Esc 撤销。
- 右侧 Inspector 集中显示设备、分区、用途和关键操作，当前默认系统时切换按钮禁用并提供原因提示。
- Windows 11 启用 DWM 系统背景材质和圆角，Windows 10 使用高对比半透明层级回退。
- 参考 OrbiEn Desktop 重构主界面：新增 72px 图标侧栏，将系统切换、设置和公告拆分为独立工作页。
- 系统切换页改为一个完整的 12px 圆角工作面板，使用内部分隔线和浅色详情区取代多个独立描边卡片。
- 采用 OrbiEn 的蓝色、浅蓝灰工作区、冷白侧栏、白色面板、8px 圆角和 150ms 级状态动画，保留全部原有业务逻辑。
- 新增侧栏导航悬停、选中和键盘焦点动画，页面切换采用短距离 ease-out 过渡并跟随 Windows 动画设置。
- 新增 GitHub Release 云更新：后台检查最新正式版，用户确认后自动下载、校验、替换并重启。
- 更新失败时保留当前 EXE，不影响双系统切换功能。
- 新增公告功能：软件启动和每次打开公告时自动从 GitHub 仓库的 `ANNOUNCEMENT.md` 读取官方通知，无需手动刷新。
- 公告支持 Markdown 大标题、小标题、正文、列表、分隔线和 HTTPS 图片。
- 云更新保留启动后的后台自动检查，并提供顶部“检查更新”按钮供用户立即手动检查。
- 修复远程公告文件尚未发布时显示 404 的问题，改用 GitHub Contents API 并自动显示内置公告。
- 兼容部分 Windows 环境把完整 BCD 查询结果写入错误流并返回非零退出码的行为，避免启动项读取被误判为管理员权限不足。
- 默认系统和启动等待时间写入遇到非零退出码时同样回读验证，避免实际已生效却显示失败。
- 修复公告尚未完成时关闭窗口可能更新已释放控件的问题，并处理公告图片和外部链接加载失败。
- 修复主窗口在自动检查尚未完成时关闭可能更新已释放控件的问题；公告图片超时或加载失败时显示明确占位并在下次打开时重试。

## v1.3.1

- 优化按钮、系统选择、状态文字和表格选中的动画节奏，交互过渡更平滑。
- 将启动项读取移至后台任务，刷新时窗口保持响应，避免界面卡顿。
- 增加刷新并发保护，连续点击刷新不会启动重复读取。
- 修复次级按钮悬停时文字和边框变白的问题。
- README 增加界面预览图和运行说明。

## v1.3.0

- 使用 AntdUI 2.4.4 重构主窗口、启动系统表格、按钮、面板和输入控件。
- 新增选中系统详情区，突出用途备注、目标分区和当前状态。
- 统一悬停、按压、加载、选择和状态文字过渡，并跟随 Windows 动画设置。
- 使用 AntdUI 模态提示替换业务操作中的传统消息框。
- AntdUI 与 Apache 2.0 许可证均嵌入单 EXE，仍无需携带外部 DLL。

## v1.2.0

- 支持读取并修改 Windows 启动菜单等待时间（`0-999` 秒）。
- 保存启动等待时间后立即写入 BCD，不再弹出二次确认。
- 支持为启动系统保存用途备注。
- 内嵌高清 Logo、应用图标和管理员权限清单。
- 提供单文件 EXE 与便携 ZIP 发布包。
